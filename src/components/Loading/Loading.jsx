import React from "react";
import { IconWrapper, RotatingIcon } from "./styles";

const Loading = () => {
  return (
    <IconWrapper>
      <RotatingIcon />
    </IconWrapper>
  );
};

export default Loading;
